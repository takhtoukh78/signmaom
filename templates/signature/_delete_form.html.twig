<form method="post" action="{{ path('app_signature_delete', {'id': signature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signature.id) }}">
    <button class="btn">Delete</button>
</form>
